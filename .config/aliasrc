# JCF's aliasrc

alias vim=nvim

# Global aliases
alias -g G="| grep"
alias -g L="| less"
alias -g cfg="/usr/bin/git --git-dir=$HOME/.cfg/ --work-tree=$HOME"

alias -g emax='emacsclient -t'
alias -g smacs='sudo emacsclient -t'

# Suffix aliases
alias -s txt=code
